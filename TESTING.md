# FlowRadio 测试指南

本文档说明了当前系统的主要功能测试点及测试方法。

## 1. 环境准备

在开始测试前，请确保以下服务已正确启动：

*   **后端 (Go)**:
    *   目录: `backend/`
    *   启动命令: `go run .`
    *   **检查点**: 确认终端输出 `✅ HTTP + WebSocket 服务器运行在 :8080` 且无 `undefined: time` 等编译错误。
    *   **配置**: 确认 `bili-coze-panel/.env` 中包含有效的 `COZE_API_TOKEN` 和 `COZE_WORKFLOW_ID`。

*   **前端 (Electron)**:
    *   目录: `flowradio-ui/`
    *   启动命令: `npm start`
    *   **检查点**: 应用程序窗口正常打开，控制台无 `net::ERR_CONNECTION_REFUSED` 错误（连接后端成功）。

---

## 2. 待测试功能列表

### 2.1 弹幕欢迎与互动 (Coze API 集成)

*   **功能描述**: 当直播间有新观众进入或发送弹幕时，系统应能通过 Coze 工作流生成欢迎语或回复。
*   **测试方法**:
    1.  **模拟测试**: 在项目根目录运行测试脚本：
        ```powershell
        python test_coze_danmu_parsing.py
        ```
    2.  **真实环境测试**:
        *   启动 Bilibili 爬虫（如果已集成）。
        *   在直播间发送弹幕。
*   **预期结果**:
    *   后端日志显示 `📥 收到前端转发的弹幕总结` 或 `🚀 [Coze] 发起请求`。
    *   Coze API 返回 `ret: 0` 及生成的回复内容。
    *   前端播放生成的语音 (TTS)。

### 2.2 自动切歌逻辑 (Auto Music Changer)

*   **功能描述**: 当系统检测到长时间（3分钟）无音乐请求且无语音播报时，自动触发切歌流程。
*   **测试方法**:
    1.  启动后端服务。
    2.  保持系统静默，不发送任何弹幕，也不进行任何对话交互。
    3.  等待约 3 分钟。
*   **预期结果**:
    *   后端日志出现 `⏰ 触发自动切歌`。
    *   系统自动向 Coze 发送 `Chat_trigger: true` 请求，内容为 "该切歌了"。
    *   Lyria 音乐生成模块收到新的音乐参数并切换风格。

### 2.3 UI 系统控制按钮

*   **功能描述**: 前端界面的 "Start System" 和 "Stop System" 按钮应能控制后端服务的启停。
*   **测试方法**:
    1.  点击 **Stop System**:
        *   预期: 后端 WebSocket 连接断开，前端显示连接失败或重连中。
    2.  点击 **Start System**:
        *   预期: 后端服务重新启动，前端自动重连成功，控制台日志恢复正常。

### 2.4 话题 (Topic) 输出解析

*   **功能描述**: 识别 Coze 返回的 `topic: xxx` 格式内容，并将其作为特殊话题消息处理。
*   **测试方法**:
    *   观察 Coze 返回的内容。如果内容以 `topic:` 开头，前端应将其显示为话题卡片或特殊样式（取决于前端实现），而不是普通的主持人对话。
    *   后端日志应显示 `Prefix: Topic:` 而不是 `Mao:` 或 `Baobab:`。

---

## 3. 常见问题排查

*   **错误**: `undefined: time`
    *   **原因**: `backend/websocket_handler.go` 缺少 `time` 包导入。
    *   **解决**: 已修复，请重启后端。

*   **错误**: `net::ERR_CONNECTION_REFUSED`
    *   **原因**: 后端服务未启动或端口被占用。
    *   **解决**: 检查 8080 端口占用情况，或使用 UI 的 "Start System" 按钮重启。

*   **错误**: Coze API 返回 `4000` 或 `5000`
    *   **原因**: 请求参数格式不正确（如缺少 `danmu` 数组）。
    *   **解决**: 确保使用最新的 `coze_client.go` 代码，它已适配新的 Payload 结构。
